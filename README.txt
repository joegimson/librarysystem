menu.py can be used to search for, checkout and return books.
bookweed.py is used to visualise unpopular book titles.

NOTE:line 9 of bookweed.py must be edited if a new book is added to logfile.txt
add 2 new elements into the list but do not add multiples of 3.
Similarly definition of fix_database() in database.py must also be changed if a book is deleted or added to database.txt.
Instructions can be found inside database.py file.

When adding records to database.txt and logfile.txt each element MUST be seperated by a tab.